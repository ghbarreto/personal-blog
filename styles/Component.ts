import styled from 'styled-components';
import * as utilities from './utilities';

export const Component = styled.div`
  background-color: ${utilities.bgColor};
  height: auto;
  max-width: 1600px;
  margin: 0 auto;
  margin-top: 40px;
  border-radius: 30px;
`;
